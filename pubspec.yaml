name: bachelor_flutter_crush
description: Eine Studie der Kammer für Arbeiter und Angestellte für Niederösterreich

# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
version: 1.0.11

environment:
  sdk: ">=2.16.0-116.0.dev <3.0.0"

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  just_audio: ^0.9.40
  video_player: ^2.9.1
  rxdart: ^0.28.0
  shared_preferences: ^2.3.2
  flutter_bloc: ^8.1.6
  flutter_unity_widget: ^2022.2.1
  pointer_interceptor: ^0.10.1+2
  fluttertoast: ^8.2.8
  dependency_validator: ^4.1.0
  universal_html: ^2.2.4
  pwa_install: ^0.0.5
  survey_kit: ^0.2.0
  intl: ^0.18.1
  url_launcher: ^6.3.0
  device_info_plus: ^10.1.2
  permission_handler: ^11.3.1
  firebase_core: ^3.4.1
  firebase_messaging: ^15.1.1
  firebase_database: ^11.1.2
  firebase_storage: ^12.3.0
  flutter_native_splash: ^2.4.1

  flutter_web_plugins:
    sdk: flutter


dev_dependencies:

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^4.0.0
  flutter_launcher_icons: ^0.13.1
  test: ^1.24.9 #Do Not upgrade

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter.

flutter_icons:
  android: "launcher_icon"
  ios: true
  image_path: "assets/icons/favicon-32x32.png"
  remove_alpha_ios: true

flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  assets:
    - assets/audio/Background_Music.mp3
    - assets/audio/losing_music.mp3
    - assets/audio/winning_music.mp3
    # images
    - assets/images/background/background_new.png
    - assets/images/background/background_new2.png
    - assets/images/bombs/jelly_gelb.png
    - assets/images/bombs/jelly_bunt.png
    - assets/images/bombs/jelly_gelb_vertical.png
    - assets/images/bombs/jelly_gelb_horizontal.png
    - assets/images/login/login-page.png
    # Instructions
    - assets/instructions/ak_uni_logo.png
    - assets/instructions/welcome_1.png
    - assets/instructions/welcome_2.png
    - assets/instructions/ins_1.png
    - assets/instructions/ins_2.png
    - assets/instructions/ins_3.png
    - assets/instructions/ins_4.png
    - assets/instructions/ins_6_activated.png
    - assets/instructions/ins_6_deactivated.png
    - assets/instructions/ins_6_competition.png
    - assets/instructions/ins_6_FoMo.png
    - assets/instructions/ins_6_1_activated.png
    - assets/instructions/ins_6_1_deactivated.png
    - assets/instructions/ins_6_1_competition.png
    - assets/instructions/ins_6_1_FoMo.png
    - assets/instructions/InstallAndroid.png
    - assets/instructions/installios.png
    - assets/instructions/InstallAndroid_alt1.png
    - assets/instructions/InstallAndroid_alt2.png
    # levels
    - assets/unityLevels.json
    # icons
    - assets/icons/android-chrome-512x512.png
    # videos
    - assets/videos/werbung_ak.mp4

flutter_native_splash:
  image: assets/instructions/splash_screen.png
  color: "#42a5f5"
